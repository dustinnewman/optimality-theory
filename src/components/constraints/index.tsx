import React from "react";
import AddConstraint from "./add";
import Rankings from "./rankings";

const Constraints = () => (
  <section>
    <h1>Constraints</h1>
    <Rankings />
    <AddConstraint />
  </section>
);

export default Constraints;
